
name: libro-dashboard
root: ~/Code/libro/dashboard-web

startup_window: editor

windows:
  - console:
      panes:
        - sleep 5; git pull; nvm use && yarn install && yarn start --ssl
        - git status
  - editor:
      layout: main-horizontal
      panes:
        - vim
