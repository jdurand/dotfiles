
name: libro-widgets
root: ~/Code/libro/widgets-web

startup_window: editor

windows:
  - console:
      panes:
        - sleep 10; git pull; yarn install && yarn start --ssl
        - git status
  - editor:
      layout: main-horizontal
      panes:
        - vim
