# /Users/jdurand/.config/tmuxinator/obb.yml

name: obb
root: ~/OpenBBUserData

windows:
  - obb:
      root: ~/OpenBBUserData
      # run this to capture current layout:
      # - tmux list-windows -F "#{window_active} #{window_layout}" | grep "^1" | cut -d " " -f 2
      layout: af09,323x74,0,0{110x74,0,0,65,212x74,111,0[212x7,111,0,66,212x55,111,8,67,212x10,111,64,68]}
      # layout: main-vertical
      panes:
        - obb
        -
        - obb
        - cd ~/Code/oss/OpenBBTerminal && git status
